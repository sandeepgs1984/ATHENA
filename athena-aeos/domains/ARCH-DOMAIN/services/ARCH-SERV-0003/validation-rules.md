# Validation Rules

The Architecture Validator SHALL verify:

- Required documents exist
- Repository structure is valid
- Layer dependencies follow approved rules
- No cyclic dependencies exist
- Naming conventions are satisfied
- Architecture principles are respected
- Required metadata is present

Validation rules are deterministic and reusable.