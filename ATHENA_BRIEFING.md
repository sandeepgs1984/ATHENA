# ATHENA — Agent Briefing

**Read this file first, in every session, before touching any code or docs.**
It is a map, not a snapshot: it tells you what ATHENA is and exactly where to
look for facts that change over time. Nothing about *current status* is
hardcoded here — status lives in `docs/MILESTONES.md` and
`IMPLEMENTATION_SUMMARY.md`, and this file always defers to them.

---

## 1. What ATHENA is (stable — rarely changes)

ATHENA is a **single-user, advisory-only decision-intelligence platform** for
one trader's NSE/BSE swing and intraday equity trading. It is not a screener,
not a bot, and **never places or executes trades**. It ingests market data,
runs a deterministic analytical pipeline (regime → market health → evidence →
indicators → score → confidence → risk → decision), and produces fully
explainable `Decision` objects (TRADE / WATCH / NO_TRADE / etc.) with a
persisted reasoning trace. The owner reads the Decision Brief, decides, and
executes manually through their own broker. The "learning engine proposes,
the trader approves" — ATHENA never acts autonomously on capital.

## 2. Non-negotiable invariants (frozen — do not weaken, ever)

- **No order-placement code anywhere in this repo.** Not a flag, not a
  "future" stub, not behind a feature flag. Grep for it before you trust a
  claim that it doesn't exist.
- **Every number ATHENA shows is explainable.** Explanations are computed and
  *persisted as data* by the engine that produced the value (ADR-005) — the
  API/dashboard render only; they never reconstruct or recompute a rationale.
- **Deterministic and replayable.** Injected clocks, `Decimal` money,
  timezone-aware timestamps, no hidden state, no `random`/`datetime.now()`
  inside analytical engines.
- **Provider independence.** Broker/price-data concerns live behind
  `MarketDataProvider`/broker abstraction Protocols (ADR-002);
  institutional cash-flow (FII/DII) is a separate Protocol under ADR-008
  — business logic never imports a concrete provider.
- **Architecture is FROZEN.** `ATHENA-002-System-Blueprint.md` is the single
  source of truth for module boundaries. A genuine architectural limitation
  means: stop, write an ADR proposal, wait for owner approval — never
  silently work around it.
- **Secrets live only in `.env`** (gitignored). Never in `config/*.json`,
  never committed.

## 3. Orientation checklist — do this before any work

1. Read this file (you're doing it).
2. Read `CLAUDE.md` (a.k.a. `AGENTS.md`) — the mandatory process rules:
   git-action restrictions, commit message format, milestone workflow,
   implementation discipline. These override your default behavior.
3. Read `docs/MILESTONES.md` in full (it's compact) — the live roadmap. It
   tells you what's Approved, what's Ready for review / In design, what's
   Deferred, and what (if anything) is currently in flight. **Never assume a
   milestone's status from memory, from IMPLEMENTATION_SUMMARY.md's title,
   or from this briefing — this table is the only authority.**
4. Read the **top entry only** of `IMPLEMENTATION_SUMMARY.md` (newest-first
   log) for the full detail of the most recent milestone: scope, files
   touched, tests, risks, remaining work. Do not read the whole file — it is
   a permanent, ever-growing history (100K+ words), not a briefing document.
5. Run `git log --oneline -20` and `git status` to see what's actually
   committed vs. still sitting in the working tree, and cross-check against
   step 3/4 — the docs are updated by the AI before commit, so a milestone
   can be documented as done before the user has actually committed it.
6. Only then start work — and only on the single milestone the owner has
   authorized. If asked to "continue" or "what's next," answer from steps
   3–5, don't guess.

## 4. Governing documents — what's in each, and when to open it

| Document | What it contains | Open it when... |
|---|---|---|
| `ATHENA-000-Master-Architecture.md` | The constitution: why ATHENA exists, product philosophy, what it must never do | You need to justify a product-level decision or check a first-principles rule |
| `ATHENA-001-Engineering-Review.md` + `ATHENA-001R-Owner-Review.md` | Engineering review across 7 roles, disagreements resolved, accepted amendments | You need the *reasoning* behind an engineering rule (referenced as "ATHENA-001 amendment N") |
| `ATHENA-002-System-Blueprint.md` + `ATHENA-002R-Owner-Review.md` | The frozen architecture: module boundaries, phase plan, Definition of Done per phase | Before writing **any** code — confirm the relevant section and Definition of Done |
| `docs/adr/ADR-0*.md` | Architecture Decision Records: modular monolith, broker abstraction, pipeline context, static-HTML-first, explainability-as-data, circuit limits, owner-triggered background validation, institutional-flow provider (ADR-008 Accepted), repository read-concurrency (ADR-009 Accepted), DarvaX satellite module (ADR-010 Accepted, + Amendment 1 dashboard tab Accepted, Amendment 2 universe screening Accepted), canonical symbol master + scanner-specific universes (ADR-011 Accepted 2026-08-15), isolated Explosive Move Radar research boundary (ADR-012 Accepted 2026-08-21), Entry Qualification architecture boundary (ADR-013 Accepted 2026-09-02), EMR live-shadow operation (ADR-014 Accepted 2026-09-03), and Intraday Actionability architecture boundary (ADR-015 Accepted 2026-09-04) | You're unsure whether a pattern is intentional or need to cite the decision that made it so |
| `docs/decisions/DD-*.md` | Deferred Decision resolutions (e.g. DD-1 broker/data vendor = Zerodha Kite Connect, DD-9 alerting = webhook+file, DD-11 FII/DII institutional flow) | Before assuming something is "still undecided" — check here first, it may already be resolved |
| `docs/design/F5-MARKET-HEALTH-SCORE.md` | Exact F-5 six-component Market Health Score contract (formulas, unknown-data policy, persistence, consumer alignment) | Changing F-5 formulas/weights/unknown-data policy, or debugging Market Summary Unavailable states |
| `docs/design/DARVAX-CONFIGURATION.md` | Complete `config/darvax.json` reference: the ATHENA/DarvaX ownership boundary (ATHENA reads only `enabled`), enable/disable procedure, every key with default + range + deck provenance, which blocks are omitted-but-live, methodology-digest implications, and every failure mode | Turning the DarvaX satellite on or off, changing any DarvaX parameter, or diagnosing why DarvaX did or didn't mount |
| `docs/design/DARVAX-PERFORMANCE-EVIDENCE.md` | DX-4a measured evidence: what mounting DarvaX costs ATHENA (nothing detectable), what a scanning DarvaX costs at realistic vs worst-case cadence, the state-ordering confound and how it was controlled, and why no mitigation is recommended | Judging whether an enabled DarvaX is affecting ATHENA responsiveness, or before proposing any DarvaX isolation/queueing work |
| `docs/design/DARVAX-VALIDATION-EVIDENCE.md` | DX-5 validation: why DarvaX's `EXPERIMENTAL_UNVALIDATED` label still stands, the sufficiency gate that enforces it, the measured 1%-vs-10% stop comparison, and the `ingestion.lookback_days` blocker | Before trusting any DarvaX signal, or before proposing that the experimental label be removed |
| `docs/design/SYMBOL-UNIVERSE-INVESTIGATION.md` | Why the scanner universe is 528 symbols and not the NSE equity list: the `owner_candidates` → ingest → `instruments` chain, what the Kite dump can and cannot classify (no series column; `instrument_type` is `EQ` for all 10,197 rows), the 365-day question answered, and the three-symbol validation results | Before changing any universe, eligibility filter or ingest scope, or when asking why a symbol a scanner should see is invisible to it |
| `docs/ATHENA-DARVAX-UX-ROADMAP-HANDOFF.md` | Agent handoff for picking up the UX roadmap: the mandatory read order, non-negotiable invariants restated for a fresh session, the Design→Implement→Test→Review workflow, and a "gotchas paid for the hard way" section (DarvaX test-database isolation, asset-version cache-busting, a JS escaped-em-dash string-matching trap, a duplicate-function-declaration bug, and a local-preview charset/viewport false-alarm) | You are a new agent (or session) about to start implementing a UX roadmap item and need the full context in one place |
| `docs/design/ATHENA-DARVAX-UX-ROADMAP.md` | 29 advisory-dashboard UX improvement ideas spanning ATHENA core and the DarvaX satellite, each checked against a code survey of what already exists (notifications, decision-compare, saved symbols, search, responsive coverage, personalization, the Decision Journal/outcome tracker, onboarding, keyboard shortcuts, ATHENA↔DarvaX cross-linking) so nothing reproposes a feature that's already built. A menu for the owner, not a plan — nothing here is scheduled | Deciding whether a UX idea already exists in some form before proposing it, or picking the next UX milestone |
| `docs/design/ATHENA-EXPLOSIVE-MOVE-RADAR-ROADMAP.md` | Accepted research-first EMR sequence, evidence and promotion gates, explicit train/validation/calibration/final-test discipline, performance-isolation requirements, and current EM handoff | Starting or reviewing any Explosive Move Radar milestone; confirm the current status in `docs/MILESTONES.md` first |
| `docs/ATHENA-EMR-HANDOFF.md` | Dated EMR continuity snapshot: approved milestones, measured evidence, frozen research boundaries, mandatory read order, and the exact next-milestone scope | Resuming EMR work in a new agent or session after confirming current status in `docs/MILESTONES.md` |
| `docs/ATHENA-ID-TRACK-HANDOFF.md` | Dated Intraday Intelligence (ID-track) continuity snapshot: approved milestones ID-0 through ID-6D.1 (Entry Qualification methodology-frozen, persisted, canonically Decision-bound, and workflow-wired), ID-6E's replay/shadow validation analysis owner-accepted as historically BEHAVIORALLY SOUND after ID-6E.1's Decision-episode trajectory-identity correction (corrected flicker 11.73%, superseding the pre-correction 39.76% figure), ID-6E.2's completed production schema activation and first genuine 165-row shadow canary (owner-approved/closed), ID-6E.3's read-only REGULAR-session shadow characterization (owner-approved/closed; 489 genuine REGULAR-phase rows found, zero integrity defects, classified REPLAY_SOUND_SHADOW_EVIDENCE_STILL_ACCUMULATING), the subsequent closure-gate clarification (documentation only: production issues a fresh canonical Decision per instrument per cycle, so same-Decision multi-checkpoint episodes and TRADE observation are no longer mandatory closure conditions), and a final read-only post-market shadow audit (2026-09-03, full completed session, 6,640 rows) that found all invariants hold with zero defects and classified REPLAY_AND_SHADOW_BEHAVIORALLY_SOUND — owner reviewed and accepted this audit 2026-09-03: **ID-6E OWNER APPROVED/CLOSED, and ID-6 OVERALL OWNER APPROVED/CLOSED** (entire ADR-013/ID-6A0 through ID-6E track), an **ID-7 discovery-only pass** (2026-09-03: current TradePlan is daily/ATR-only with zero intraday inputs and cannot be retrofitted to consume EntryQualification; recommends a new, separate intraday actionability artifact and a new ADR — `docs/research/ID-7-INTRADAY-ENTRY-TRADEPLAN-DISCOVERY.md`; **owner approved the discovery and accepted Option B as the architectural direction 2026-09-03; ID-7A0 not started**), **ID-7P0** (2026-09-03, narrow latency-attribution instrumentation, owner-authorized before any ADR drafting: opt-in, orthogonal wall-clock timing wired into the real scheduled cycle path, never touching `WorkflowEngine`'s own deterministic clock — `docs/research/ID-7P0-PRODUCTION-CYCLE-LATENCY-ATTRIBUTION.md`), **ID-7P0.1** (2026-09-03, same day, owner-approved/closed correction: the residual timing phase was renamed `orchestration_overhead_pre_final_persist` — it excludes the final RunRecord persist call, not fixed by an extra write — and the pacing-floor estimate was corrected using verified production `config/ingestion.json` (`timeframes:["5m","15m"]`) and real `db/athena.db` evidence (536 instruments × 3 historical calls) from an unverified ≈63% to a verified ≈**95.8%** of the observed average cycle duration; strong prior evidence, not yet measured proof), and the subsequent **owner-authorized production restart** (2026-09-03, safety-verified: no active cycle interrupted, zero artificial runs/provider calls/Decisions/EntryQualification rows created), followed by a **final read-only production latency-attribution audit (2026-09-04, after the regular NSE session closed)** classifying the ~9-10 minute cycle latency as `INGESTION_DOMINANT` / `HISTORICAL_CANDLE_PACING` from n=21 genuine `REFRESH` cycles (the deterministic `(N-1)×0.334s` pacing floor for 1,608 sequential historical calls/cycle explains ≈95.7% of the median cycle on its own; zero measured failed provider-call samples across the completed-cycle sample, internal retry count not persisted and therefore not directly known) — recommendation classification **A (latency compensation only)**, with an explicit caveat pending ID-7A0's own target-entry-timescale decision, followed by **ID-7P0.2** (2026-09-04, same day: owner accepted the root-cause attribution but held final closure for a corrective run-anomaly triage — 3 independently-caused transient network-error `FAILED` REFRESH runs and 1 orphaned `RUNNING` REFRESH run were investigated read-only; the orphan is root-caused to an owner-triggered `os.execv` service restart recovering from a stuck cycle (proven via live production log + a `config_snapshot_id='cfg-full-validation'` DB marker showing the very next run came from the ungated "Validate All" path, not the normal schedule), confirmed to carry zero current business/lock/dashboard impact; §17/§25's "zero retries" wording corrected to "zero measured failed calls, retries not directly measurable" without weakening the pacing conclusion; Recommendation A re-recorded as non-binding; ID-7A0 gate classification `NO_ID7A0_BLOCKER`), followed by **Owner/Chief Architect approval and closure of both ID-7P0.2 and ID-7P0 2026-09-04** and same-day authorization of **ID-7A0** (architecture/ADR only): status **ID-7A0 ARCHITECTURE COMPLETE — READY FOR OWNER / CHIEF ARCHITECT REVIEW; ID-7A NOT STARTED** — new `EntryActionability` artifact designed (identity = EQ's full composite key + its own `entry_actionability_as_of`/methodology_version, no surrogate id; 4-state model; canonical-cycle-synchronous evaluation mode selected; Recommendation-A reassessment `A_CANNOT_BE_DECIDED_UNTIL_ID7B`; zero methodology numerics invented), `docs/adr/ADR-015-intraday-actionability-architecture.md` (Proposed) and `docs/research/ID-7A0-INTRADAY-ACTIONABILITY-ARCHITECTURE.md` created, zero implementation, frozen boundaries, ID-5B's closed CASE A/B/C/D classification result, and a verified cross-track isolation finding vs. EMR, followed same day by **ID-7A0.1** (owner source review found a genuine lifecycle contradiction — a proposed `EXPIRED` state conflated a persisted methodology verdict with a read-time currentness judgment, which an immutable append-only row cannot express; resolved by freezing three separate dimensions — persisted 3-state methodology state `UNKNOWN/NOT_ACTIONABLE/ACTIONABLE`, a never-persisted read-time `is_currently_usable(...)` freshness predicate, and EQ-inherited finality/provisionality — and by correcting the WATCH/TRADE scope reasoning from "forced consequence" to a tested, TRADE-only evaluation decision) — **Owner/Chief Architect accepted ADR-015 and closed both ID-7A0.1 and ID-7A0, 2026-09-04**, followed same day by **ID-7B** (entry/risk methodology discovery: found zero real `(TRADE, QUALIFIED)` historical episodes in production, ran a new empirical freshness analysis on 138,454 real M5 candle-pair samples, classified canonical-cycle freshness `CONDITIONAL_ON_EVIDENCE_AGE` and ID-7P0 Recommendation A `A_CONDITIONALLY_ACCEPTED`, froze entry/invalidation/reward representation shapes and the `is_currently_usable` ingredients while explicitly deferring all numeric thresholds, and surfaced a genuine upstream EQ long-bias gap — status **METHODOLOGY PARTIALLY FROZEN**, Owner-reviewed/accepted-as-is 2026-09-04, `docs/research/ID-7B-ENTRY-RISK-METHODOLOGY.md`), followed same day by **ID-7B.1** (read-only retrospective reconstruction: real historical TRADE decisions replayed through the unmodified `EntryQualificationEngine` at their own checkpoints, reusing the ID-6E harness pattern — 96,985 decisions → 6,624 episodes → 783 real TRADE+`REPLAYED_QUALIFIED` target-cohort episodes, 100% reconstruction success; sharper SHORT finding — zero SHORT decisions exist anywhere in the DB; freshness/extension/invalidation/reward evidence computed descriptively on the real target cohort, discovering same-reference-level anchor+invalidation pairs are structurally degenerate; classification `TARGET_COHORT_RECONSTRUCTED_SUFFICIENT`, status `READY_FOR_ID7B2_CALIBRATION`), `docs/research/ID-7B1-RETROSPECTIVE-TRADE-EQ-RECONSTRUCTION.md`) — **Owner-reviewed and accepted 2026-09-04**, followed same day by **ID-7B.2** (chronological session-grouped calibration/validation, 14-discovery/6-validation split frozen before any outcome inspected): established the first real comparison-population evidence (QUALIFIED materially outperforms non-QUALIFIED on both folds, separation strengthens out of sample); found `EXTENSION_GATE_NOT_SUPPORTED` (extension associates with BETTER outcomes, reversing ID-7B's own hypothesis); validated VWAP-loss as primary and OR15-boundary as secondary invalidation (correcting an ID-7B.1 degenerate-pairing artifact), D1-ATR → `NO_VALIDATED_FALLBACK`; calibrated +10m as the frozen freshness/currentness band (matching ID-7P0's measured cycle duration); RR → `RR_INFORMATIONAL_ONLY`; RS/RVOL/Gap → `CONTEXT_ONLY`. Classified `OPTION1_ACCEPTABLE_WITH_STRICT_CURRENTNESS`, Recommendation-A `A_ACCEPTED_ONLY_WITH_CURRENTNESS_GUARD`, `LONG_VALIDATED_SHORT_UNVALIDATED`. Froze the complete V0 methodology contract and minted a methodology version — status **`V0_METHODOLOGY_CALIBRATED_AND_VALIDATED`, READY FOR OWNER / CHIEF ARCHITECT REVIEW, NOT YET OWNER-APPROVED**, `docs/research/ID-7B2-ENTRY-RISK-CALIBRATION-VALIDATION.md` — **Owner accepted the calibration evidence but held the final contract freeze for three consistency corrections** (2026-09-04, same day), followed by **ID-7B.2.1**: corrected §14's entry/invalidation pairing (it had recreated the exact `VWAP-anchor + VWAP-loss` degeneracy §8 itself forbids — entry trigger is now the M5-close checkpoint price, VWAP is location-context only, OR15-boundary is an always-computed contextual secondary never substituted for VWAP-loss), removed D1 ATR from mandatory evidence, and corrected the freshness predicate to `now − evidence_as_of` (was collapsed into `entry_actionability_as_of`) — zero calibration evidence or classification reopened. Status **CONTRACT CORRECTED — ID-7B.2.1 OWNER APPROVED / CLOSED 2026-09-04, and ID-7B/ID-7B.1/ID-7B.2 OWNER APPROVED / CLOSED SAME DAY (V0 methodology fully frozen)**, followed same day by owner-authorized **ID-7A** (domain model + persistence, not the evaluator/workflow), completed 2026-09-05 as **ID-7A IMPLEMENTATION COMPLETE — READY FOR OWNER / CHIEF ARCHITECT REVIEW** (immutable `EntryActionability` domain object + separate pure `is_currently_usable` currentness helper, `SCHEMA_VERSION` 17→18, append-only dual-binding-validated repository methods, 75 new tests, full suite 3455 passed/1 pre-existing skip — no evaluator/workflow/replay/production rows, not marked Owner-approved), followed by **ID-7A.1** (2026-09-05, owner source-review correction: domain-integrity hardening closing the gap between "repository binding proves copied fields match real records" and "those fields form a legal verdict" — rejects untruthful ACTIONABLE/NOT_ACTIONABLE/UNKNOWN combinations, PIT causal-ordering violations, and a currentness future-evidence bug; `SCHEMA_VERSION` stays 18; 29 new tests; full suite 3484 passed/1 pre-existing skip; status **ID-7A.1 COMPLETE — ID-7A READY FOR OWNER / CHIEF ARCHITECT CLOSURE REVIEW**) | Resuming ID-track work in a new agent or session, and mandatory reading (alongside `docs/ATHENA-EMR-HANDOFF.md`) before any further Entry Qualification milestone |
| `docs/design/EM-5-LIVE-SCANNER-CONTRACT.md` | EM-5 live-scanner design contract (proposed 2026-08-28, pending Owner approval): frozen-artifact promotion/loading, the exact state-transition table, eligibility/feasibility gates, ranking/probability-language rules, bulk-data/isolation design (mirrors DarvaX's ADR-010 pattern), replay determinism, performance/canary gates, persistence schema, and required tests | Before writing any EM-5 scanner code, or reviewing/approving that contract |
| `docs/research/EM-1A-DATA-COVERAGE-AUDIT.md` | Measured production-ledger coverage, survivorship and corporate-action risks, intraday integrity findings, frozen event/checkpoint contract, and the remediation gate that currently leaves zero checkpoints accepted | Reviewing EM-1a or deciding whether EM-1b can safely generate labels |
| `docs/research/EM-1R2-CORPORATE-ACTION-COVERAGE-CONTRACT.md` | Frozen official-NSE authority, survivor-cohort, identity-resolution, exclusion, and replay rules for EM-1r2 | Working on EMR corporate-action acquisition or downstream event-window admission |
| `docs/research/EM-1R2-CORPORATE-ACTION-COVERAGE-REPORT.md` | Measured EM-1r2 interval, cohort, actions, exclusions, replay evidence, and limitations | Reviewing EM-1r2 status or handing off to the next EM remediation milestone |
| `docs/design/EM-1-RESEARCH-DATA-REMEDIATION-PLAN.md` | Approval-gated EM-1r sequence for authoritative corporate actions, canonical intraday sessions, point-in-time cohort/quote hygiene, provenance, and checkpoint re-admission | Reviewing EM-1r1 or designing any EM data-remediation milestone before EM-1b |
| `docs/research/PS-P0-PORTFOLIO-SYNC-DISCOVERY-REPORT.md` + `docs/research/PS-P1-PORTFOLIO-CONTRACT-DESIGN.md` + `docs/research/PS-P2-PORTFOLIO-IMPORT-RECONCILIATION.md` + `docs/research/PS-P3-MY-PORTFOLIO-DASHBOARD-UX.md` + `docs/research/PS-P4-PORTFOLIO-SYNC-ORCHESTRATION.md` + `docs/research/PS-P5A-PORTFOLIO-INTERPRETATION-METHODOLOGY.md` + `docs/research/PS-P5B-PORTFOLIO-INTERPRETATION-IMPLEMENTATION.md` + `docs/research/PS-P6C-MY-PORTFOLIO-V1-END-TO-END-VALIDATION.md` + `docs/research/PS-P7A-PORTFOLIO-INTELLIGENCE-V2-DISCOVERY.md` + `docs/research/PS-P7B-PORTFOLIO-CONVICTION-ADAPTER-IMPLEMENTATION.md` + `docs/research/PS-P8A-PORTFOLIO-TREND-SETUP-METHODOLOGY-DISCOVERY.md` + `docs/research/PS-P8B-PORTFOLIO-D1-TREND-METHODOLOGY-FREEZE.md` + `docs/research/PS-P8C-PORTFOLIO-D1-TREND-ADAPTER-IMPLEMENTATION.md` + `docs/research/PS-P9A-PORTFOLIO-SETUP-METHODOLOGY-DISCOVERY.md` + `docs/research/PS-P9B-PORTFOLIO-OPENING-RANGE-SETUP-METHODOLOGY-REPLAY.md` | Portfolio Sync / My Portfolio V1 discovery through final freeze, plus Portfolio Intelligence V2 Conviction and D1 Trend implementation and Portfolio Setup methodology/replay: isolated source of truth, CSV/XLSX preview, canonical symbol resolution, atomic confirmation, stale-preview recovery, immutable analysis snapshots, factual current-holdings display, server-owned valuation math, expected-session freshness/provenance, coherent Decision/TradePlan evidence, frozen V1 portfolio interpretation, holdings-digest currentness, final V1 validation, V2 evidence/methodology inventory, PS-P7B's Conviction adapter from coherent persisted Decision Confidence HIGH/MEDIUM/LOW only, PS-P8A's owner-approved finding that Trend and Setup are separate dimensions, PS-P8B's frozen D1 Trend methodology, PS-P8C's owner-approved D1 Trend adapter populating only UPTREND/DOWNTREND/MIXED under portfolio-interpretation-v2, PS-P9A's frozen Setup semantic/evidence-role decisions, and PS-P9B's replay verdict that raw Opening Range events are not yet safe as production Setup until directionality, OR15/OR30 precedence, returned-inside behavior, and lifecycle are frozen | Starting or reviewing any My Portfolio / Portfolio Sync / Portfolio Intelligence milestone; confirm current PS status in `docs/MILESTONES.md` first |
| `docs/ATHENA-TECHNICAL-ARCHITECTURE.md` | Complete implementation reference: full tech stack, backend architecture (domain model, config system, provider abstraction, persistence, calendar, observability, explainability-as-data), the real decision-pipeline module map (with corrections to the "17 Protocol interfaces" and "11-stage pipeline" framings), the API layer (auth, versioning, error model, the DarvaX satellite-mount pattern), the frontend (static HTML/vanilla JS assembly, the actual chart stack vs. the aspirational "Lightweight Charts" docs), testing/tooling/ops, and a dedicated "known documentation/implementation gaps" section | You need to know how ATHENA is actually built rather than how a formula works, are onboarding to the codebase, or are about to state a claim about the stack/architecture that this document can verify or correct |
| `docs/ATHENA-WORKFLOW-METHODOLOGY.md` | End-to-end reference: full 11-stage decision pipeline with every formula/threshold/weight currently in config, scheduling cadences, dashboard workflow, and an explicit "Known Gaps" section (declared-but-dormant code paths, proposed-but-unimplemented ADRs) | You need the exact current formula/threshold for any engine, or want to know what's actually live vs. only declared in the architecture, without re-deriving it from source |
| `docs/api/openapi.yaml` + `postman_collection.json` + `API-REFERENCE.md` | The full REST API surface (81 operations, generated from the live `/api/openapi.json`), a ready-to-import Postman collection, and a human-readable walkthrough (auth flow, envelope/error model, per-resource examples) | Integrating against the API, or checking a route/parameter/schema exists — regenerate from `/api/openapi.json` whenever routes change, since these are snapshots, not live-synced |
| `docs/ATHENA-IX-HANDOFF.md` | Dated IX-track continuity snapshot: implemented data flow, runtime contracts, QA evidence, known unavailable states, and remaining approval gates | Picking up IX work after verifying current status in Milestones, Implementation Summary, and git |
| `docs/PRODUCTION_READINESS_ROADMAP.md` | R1–R6 daily-advisory-use readiness tracks | Working on live/ops-facing concerns |
| `docs/ops/*.md` | Runbooks: file-backed daily ops, Kite live data, host schedule/alerts, live-entry, QA verification | Doing anything with the live workstation, Kite auth, or scheduled cycles |
| `README.md` | Human-facing landing page: doc index, workstation entry commands, core stack decisions | Quick doc-index lookup or the exact `athena-serve` invocation |
| `CLAUDE.md` / `AGENTS.md` (symlink) | Mandatory AI process rules — git actions, commit format, milestone workflow | Every session, before doing anything |

## 5. How to determine current status — procedure, not a fact

Status changes constantly; this section never states a status, only the
procedure to find it, so it can't go stale:

1. `docs/MILESTONES.md` → find the track/phase table → read the **Status**
   column. Legend: ✅ Approved · 🔄 Ready for review / In design ·
   ⏸ Deferred · ⏳ Planned. A track is "closed" only when its heading says so
   explicitly (e.g. "*Foo track closed (date):* ...").
2. If a milestone shows 🔄, check whether it's actually still in flight or
   just awaiting a final "approve" from the owner in-chat — search recent
   conversation/commit history for the milestone's name.
3. `IMPLEMENTATION_SUMMARY.md` top entry → full detail on whatever
   `docs/MILESTONES.md` says is most recent.
4. `git log --oneline -20` → what's actually committed. The docs can be
   ahead of git (AI documents before the owner commits) or, less often,
   behind it.
5. Never state "ATHENA is at phase/milestone X" without having just done
   steps 1–4 in *this* session — a memory or a prior summary is not a
   substitute for reading the live files.

## 6. Repo map (current shape — see `src/athena/` for the authoritative list)

Non-code:
- `config/*.json` — all runtime configuration. Never hardcode a threshold,
  weight, or path that belongs here.
- `data/index_constituents/<effective-date>/` — immutable official NSE index
  membership snapshots plus checksum/provenance manifest; never overwrite an
  existing effective-date directory.
- `tests/` — mirrors `src/athena/` structure, 1000+ tests. Full suite must
  pass before any milestone is considered done.
- `docs/` — see table above.
- `IMPLEMENTATION_SUMMARY.md`, `docs/MILESTONES.md` — status (§5).

`src/athena/` grouped by concern (a directory existing here doesn't imply
its milestone is approved — check §5):

| Concern | Packages |
|---|---|
| Symbol master & universes (ADR-011) | `symbols/` — canonical `symbol_master` with series/board provenance, dated `symbol_group` membership, config-driven universe resolver, named eligibility profiles, coverage planner. Scanners reference a universe *name*; DarvaX reads a materialised universe as data through its port |
| Foundations | `domain/` (frozen canonical model), `config/`, `calendar/` (sole trading-day/session authority), `observability/` (logging/metrics/health, plus ID-7P0's `timing.py` — pure, orthogonal, observational-only wall-clock cycle-phase instrumentation, deliberately separate from `WorkflowEngine`'s own deterministic per-stage `_MonoClock`), `errors.py` |
| Data | `data/` (providers incl. institutional flow file/NSE adapters, official NSE corporate-action adapter and bounded ingestion, immutable EMR intraday source capture/replay orchestration, validation, corporate actions, strict versioned index-constituent loader, SQLite `store/repository`) |
| Market Intelligence | `regime/`, `market_health/` (categorical + F-5 score), `sector_health/`, `universe/`; dashboard `GET /api/v1/market/summary` (MH-3) |
| Decision Intelligence | `evidence/`, `indicators/`, `scoring/`, `confidence/`, `risk/`, `decision/`, `reporting/` (decision trace explanations *and* generic operational reports) |
| Intraday Intelligence (ID-track, evidence + contracts + a pure Entry Qualification engine, now persisted AND wired into the canonical runtime — no API/UI, no Decision gate) | `session/` (ID-1: `SessionContext`, completed-candle rule, `session_day_start`/`session_open_close_ts`/`canonical_slot_candles` — the shared completed + canonical-slot authorities every consumer below reuses) and `intraday/` — `IntradaySignalSet` (ID-2, formalizes existing VWAP/5m-15m-confluence evidence), `OpeningRangeEvidence` (ID-3/ID-3.1: OR15/OR30, canonical-slot-integrity-corrected), `RelativeStrengthContext` (ID-4/ID-4.1, Owner-approved: stock-vs-sector/market point-in-time comparative performance, not RSI — comparative dimensions data-blocked pending ID-5's index M5 remediation, check `docs/MILESTONES.md`), `GapContext` (ID-5C, Owner-approved/CLOSED: previous-trading-session-close → current-session-open price transition, D1-only, deliberately independent of ID-5B's now-closed live M5 semantics question), `RelativeVolumeContext` (ID-5D methodology Owner-accepted; ID-5D.1 owner-approved/CLOSED: corrected current-window contiguity + retrieval-policy correctness fixes on top of ID-5D's accepted cumulative same-time-of-day relative volume, no hardcoded baseline-length cap, canonical-M5-only so deliberately independent of ID-5B too), `EntryQualification` contracts (ID-6A: immutable state/evidence-finality/confirmation/reason/evidence-ref types bound to canonical `Decision`), `EntryQualificationEngine` (ID-6B.2/ID-6B.2A, owner-approved/CLOSED: pure, deterministic, side-effect-free v0 readiness evaluator for the owner-frozen ID-6B.1B methodology — VWAP positive AND aggregate trend BULLISH AND (RS support OR RVOL support) — plus input-coherence hardening), and `resolve_evidence_finality` (ID-6D: pure `EntryEvidenceFinality` resolver reusing the engine's own public structural/lifecycle eligibility gate, never its tri-state formula). ID-6C/ID-6C.1 (owner-approved/CLOSED) added durable append-only persistence — `entry_qualifications` SQLite table lives in `data/store/schema.py` (FK-bound to `decisions`, plus repository-level canonical-Decision-binding validation), read/write API on `SqliteRepository`; design note `docs/design/ID-6C-ENTRY-QUALIFICATION-PERSISTENCE.md`. ID-6D/ID-6D.1 (owner-approved/CLOSED) wire the whole chain into `OwnerValidationPipeline._scan_eligible`'s per-instrument `WorkflowStage` graph as a new `entry_qualification` stage (depends on `decision` + `intraday_analytics`) — persists WATCH/TRADE observations every real cycle, with `persisted_at` sourced from an injectable orchestration-boundary wall clock (`OwnerValidationPipeline`'s `persistence_clock`), deliberately independent of `EntryQualification.as_of`; design note `docs/design/ID-6D-ENTRY-QUALIFICATION-WORKFLOW-INTEGRATION.md`. `src/athena/data/id6e_replay_shadow_validation.py` (ID-6E, historical replay validation owner-accepted as BEHAVIORALLY SOUND after ID-6E.1's Decision-episode trajectory-identity correction; ID-6E.2 activated the production `entry_qualifications` schema, owner-approved/closed; ID-6E.3 read-only characterized the first genuine REGULAR-phase shadow observations (489 rows, 0 integrity defects, 0 multi-checkpoint Decision episodes yet, 0 TRADE observations yet), owner-approved/closed, classification REPLAY_SOUND_SHADOW_EVIDENCE_STILL_ACCUMULATING; a subsequent closure-gate clarification (documentation only) established that production issues a fresh canonical Decision per instrument per cycle, so same-Decision multi-checkpoint episodes and TRADE observation are no longer mandatory ID-6E closure conditions; a final read-only post-market shadow audit (2026-09-03, full completed session, 6,640 rows across 28 checkpoints) then found all invariants hold exactly with zero binding/integrity defects and classified REPLAY_AND_SHADOW_BEHAVIORALLY_SOUND; **owner reviewed and accepted 2026-09-03: ID-6E OWNER APPROVED/CLOSED, and ID-6 OVERALL OWNER APPROVED/CLOSED** (the entire ADR-013/ID-6A0 through ID-6E track) — CHECK `docs/MILESTONES.md` FOR CONFIRMATION) is a read-only research harness — never imported by any production path — that replays the real closed engine deterministically over historical market data (trajectory grouping keyed on canonical `decision_id`, not `decision_type`, per ID-6E.1) and audits persisted shadow observations in `db/athena.db`; findings in `docs/research/ID-6E-ENTRY-QUALIFICATION-REPLAY-SHADOW-VALIDATION.md` and `docs/ops/ID-6E2-ENTRY-QUALIFICATION-PRODUCTION-SCHEMA-ACTIVATION.md`. ID-5B and ID-5 are owner-approved/CLOSED as of 2026-09-01 with final `CASE_B_CONTENT_CHANGES`; ADR-013, ID-6A0, ID-6A, ID-6B.0, ID-6B.1, ID-6B.1A, ID-6B.1B, ID-6B.2, ID-6B.2A, ID-6C, ID-6C.1, ID-6D, ID-6D.1, ID-6E.1, ID-6E.2, ID-6E.3, ID-6E overall, and ID-6 overall are all owner-approved/CLOSED as of 2026-09-03 (final classification REPLAY_AND_SHADOW_BEHAVIORALLY_SOUND) — check `docs/MILESTONES.md` for confirmation. **ID-7 DISCOVERY OWNER APPROVED / CLOSED, OPTION B ACCEPTED, ID-7A0 NOT STARTED** (2026-09-03): `docs/research/ID-7-INTRADAY-ENTRY-TRADEPLAN-DISCOVERY.md` found current `TradePlan` (`domain/decision.py:14-36`) is daily-only/ATR-multiple with zero intraday-evidence inputs, structurally unable to consume `EntryQualification`, and recommended a new, separate `(decision_id, entry_qualification identity)`-bound intraday actionability artifact (naming deferred to ID-7A0) plus a new ADR. **ID-7P0 READY FOR OWNER / CHIEF ARCHITECT FINAL CLOSURE REVIEW — ROOT-CAUSE ATTRIBUTION ACCEPTED, ID-7P0.2 CORRECTIVE TRIAGE COMPLETE; ID-7P0.1 OWNER APPROVED/CLOSED** (2026-09-03/04): `src/athena/observability/timing.py` (new, pure, orthogonal wall-clock `CycleTimingRecorder`, never touching `WorkflowEngine`'s own deterministic `_MonoClock`) wired into `LiveIngestionEngine.run_cycle`/`DryRunCycleOrchestrator.run_cycle` on the real scheduled path; the diagnostic residual phase is named `orchestration_overhead_pre_final_persist` (excludes the final RunRecord persist call, per ID-7P0.1). **Owner-authorized production restart completed and safety-verified 2026-09-03** (no active cycle interrupted, zero artificial runs/provider calls/Decisions/EntryQualification rows). **Final read-only latency-attribution audit (2026-09-04, after the regular NSE session closed)**, n=21 genuine `REFRESH`/`COMPLETED` cycles from 2026-09-04 (2026-09-03's completed cycles carry no timing payload at all — cross-day replication unavailable, not required for closure): classification `INGESTION_DOMINANT` / `HISTORICAL_CANDLE_PACING` — the deterministic `(N-1)×0.334s` pacing floor for the measured 1,608 sequential historical calls/cycle (536 instruments × 3 calls: 1 daily + 2 intraday timeframes) explains ≈95.7% of the median 560.60s cycle on its own (measured historical time exceeds the floor by only ≈2%); analytical scan ≈1.6-2.0%, pre-final orchestration ≈0%; owner/chief-architect accepted this attribution. **ID-7P0.2 (2026-09-04, same day, read-only corrective triage — required before final closure):** the 3 REFRESH `FAILED` runs and 1 orphaned `RUNNING` run flagged but not investigated in the original report were resolved. The 3 FAILED runs are independent, ordinary transient network/transport errors during historical-candle calls, zero business-output impact. The orphaned `RUNNING` row is root-caused to an owner-triggered `os.execv` service restart (proven via the live production log's `409 Conflict` → `POST /api/v1/ops/restart` → same-PID restart sequence) recovering from a stuck cycle — `os.execv` bypasses Python's exception/`finally` handling entirely, explaining the missing terminal row; the very next REFRESH run is independently proven (via its own distinct `config_snapshot_id='cfg-full-validation'`) to have come from the owner-triggered, interval-ungated "Validate All" path, not a scheduling-gate defect; current impact is zero (fully superseded, no lock/Decision/EntryQualification effect, dashboard status self-resolved without ever being misleading). §17/§25's "zero retries" wording corrected to "zero measured failed calls; internal retries not directly measurable by this instrumentation" (pacing conclusion unaffected). Recommendation A explicitly re-recorded as **non-binding** — an ID-7P0 research recommendation, not an Owner-frozen ID-7 decision. **ID-7A0 gate classification: `NO_ID7A0_BLOCKER`.** Instrumentation recommended to remain permanently as low-cost observability — no code changed by either the original audit or ID-7P0.2 — `docs/research/ID-7P0-PRODUCTION-CYCLE-LATENCY-ATTRIBUTION-REVIEW.md` (§38 appended, §17/§25/§27 corrected in place). **Owner/Chief Architect approved and closed both ID-7P0.2 and ID-7P0 2026-09-04**, and authorized **ID-7A0** (architecture/ADR only) same day. **ID-7A0 (2026-09-04): OWNER APPROVED / CLOSED; ID-7A0.1 OWNER APPROVED / CLOSED; ADR-015 ACCEPTED; ID-7A NOT STARTED.** New artifact `EntryActionability` proposed — one layer downstream of `EntryQualification` (ADR-013's layer 3, WHEN/entry/risk), identity = EQ's entire composite key copied verbatim plus its own `entry_actionability_as_of`/`entry_actionability_methodology_version` (no surrogate id), directionality preserved (not long-only), four frozen timestamps (`entry_actionability_as_of`/`evidence_as_of`/`evaluated_at`/`persisted_at`) mirroring ID-6D's `persistence_clock` precedent. **Lifecycle corrected by same-day ID-7A0.1:** the persisted methodology state is 3 values (`UNKNOWN/NOT_ACTIONABLE/ACTIONABLE`) — a proposed 4th, `EXPIRED`, was removed because it conflated a persisted verdict with a read-time currentness judgment an immutable append-only row cannot express; currentness/staleness is instead a never-persisted, read-time `is_currently_usable(...)` predicate (methodology state ACTIONABLE AND bound decision_id still current AND evidence age within an ID-7B-decided threshold AND session constraints), and evidence finality/provisionality (inherited from bound EQ) is a third, independent dimension. Historical truth stays immutable (a 10:00 ACTIONABLE row still reads ACTIONABLE at 15:00, independent of whether it is `is_currently_usable` then). WATCH/TRADE scope corrected from "forced consequence of EQ binding" to a tested decision: **TRADE-only evaluation** (WATCH+TRADE was only ever the *available* upstream domain via EQ's own scope, not a mandate; surfacing WHEN/entry/risk evidence for a structurally un-authorized WATCH opportunity would misrepresent ATHENA's advisory boundary) — a WATCH-bound EQ still produces a `NOT_ACTIONABLE` row with an explicit reason, never silently omitted. Evaluation mode: **Option 1 (canonical-cycle synchronous)** selected — new `entry_actionability` `WorkflowStage` inside the existing per-instrument DAG, zero new infrastructure/provider calls; Options 2-5 evaluated and deferred (full matrix in the research report). Recommendation-A reassessment: **`A_CANNOT_BE_DECIDED_UNTIL_ID7B`** (methodology question, not architecture). Zero entry/stop/target numeric methodology invented; zero support/resistance engine invented (none exists outside isolated DarvaX); ID-9/10/11 boundaries explicitly preserved. New ADR `docs/adr/ADR-015-intraday-actionability-architecture.md` (**Status: Accepted — 2026-09-04**). Research report: `docs/research/ID-7A0-INTRADAY-ACTIONABILITY-ARCHITECTURE.md`. Zero production code/schema/workflow-stage/API/UI change. **ID-7B (2026-09-04, same day — entry/risk methodology discovery/freeze):** decisive finding — zero real `(Decision.decision_type=TRADE, EntryQualification.state=QUALIFIED)` episodes exist in production (all 96,985 TRADE decisions predate EQ persistence, which began 2026-09-03); new empirical freshness analysis (138,454 real REGULAR-session M5 candle-pair samples) found median 10-min price move 0.093% (≈3.9% of typical daily range), p90 0.351% (≈13.7%), VWAP-side persists unchanged 88.32% of the time — classified canonical-cycle freshness **`CONDITIONAL_ON_EVIDENCE_AGE`** (Option 1 remains sufficient provided the extension/chase gate and `is_currently_usable`'s evidence-age term are genuine, load-bearing gates — **no ADR-015 revision required**) and ID-7P0 Recommendation A **`A_CONDITIONALLY_ACCEPTED`**. Froze entry representation (trigger + allowable zone, VWAP-anchored), a 5-tier invalidation hierarchy (VWAP-loss → recent completed M5 extremum → Opening-Range-level-only [never breakout-event semantics, per PS-P9B's own caution] → D1 ATR fallback → UNKNOWN), reward as `GOAL_BANDS_ONLY` (confirmed `V0_DOES_NOT_REQUIRE_GENERIC_SR`), RR as informational-only, and the exact `is_currently_usable` ingredients (including exact-EQ-currentness via full composite-key equality, and that provisional evidence CAN be currently usable) — while explicitly deferring every numeric threshold (extension cutoff, zone width, lookback window, minimum RR) pending real evidence or owner risk-tolerance input. Surfaced a genuine, ID-6-owned upstream gap: EQ's frozen v0 formula is long-biased (requires VWAP ABOVE + trend BULLISH unconditionally, no symmetric SHORT path), so EntryActionability will rarely reach a genuine SHORT opportunity today — not ID-7B's to fix. No score/ML/fitting; zero provider calls; EMR/DarvaX untouched. Status **METHODOLOGY PARTIALLY FROZEN — EVIDENCE REQUIRED FOR NUMERIC THRESHOLDS** — `docs/research/ID-7B-ENTRY-RISK-METHODOLOGY.md`; **Owner-reviewed and accepted as-is 2026-09-04 (status/ADR-015 unchanged)**, followed same day by **ID-7B.1** (retrospective TRADE+EQ reconstruction research, read-only, reusing the unmodified ID-6E replay harness pattern against real historical TRADE-decision checkpoints): 96,985 TRADE decisions → 6,624 zero-invented-parameter episodes → 6,624/6,624 reconstructed (100%, zero failures) → **783 real TRADE+`REPLAYED_QUALIFIED` target-cohort episodes**; sharper SHORT finding (zero SHORT decisions exist anywhere in the DB, of any type — no population to test, root cause upstream of ID-6); target-cohort evidence 91-100% available; freshness cross-validated on real data (VWAP-side 87.11% at +10m, closely matching ID-7B's own 88.32% proxy; trend persists faster-degrading 72.98%); entry-anchor/invalidation/reward distributions computed descriptively, discovering that same-reference-level anchor+invalidation pairs are structurally degenerate; T1/T2 hit rates 23.88%/14.81%. Classification `TARGET_COHORT_RECONSTRUCTED_SUFFICIENT`, status `READY_FOR_ID7B2_CALIBRATION` (20-session chronological-split limit flagged) — `docs/research/ID-7B1-RETROSPECTIVE-TRADE-EQ-RECONSTRUCTION.md`; **Owner-reviewed/accepted 2026-09-04**, followed same day by **ID-7B.2** (chronological 14-discovery/6-validation calibration, frozen before any outcome inspected): confirmed QUALIFIED materially outperforms non-QUALIFIED, separation strengthens on validation; `EXTENSION_GATE_NOT_SUPPORTED` (reverses ID-7B's own chase-risk hypothesis — more extension associates with better outcomes); VWAP-loss validated primary invalidation, OR15-boundary validated secondary (correcting an ID-7B.1 degenerate-pairing artifact), D1-ATR → `NO_VALIDATED_FALLBACK`; +10m freshness band calibrated and validated (matches ID-7P0's measured cycle duration); RR → `RR_INFORMATIONAL_ONLY`; RS/RVOL/Gap → `CONTEXT_ONLY`; `OPTION1_ACCEPTABLE_WITH_STRICT_CURRENTNESS`; Recommendation-A `A_ACCEPTED_ONLY_WITH_CURRENTNESS_GUARD`; complete V0 contract frozen, methodology version minted — status **`V0_METHODOLOGY_CALIBRATED_AND_VALIDATED`, READY FOR OWNER / CHIEF ARCHITECT REVIEW**, `docs/research/ID-7B2-ENTRY-RISK-CALIBRATION-VALIDATION.md`; **Owner accepted the evidence, held final contract freeze for ID-7B.2.1** (2026-09-04, same day): corrected §14's degenerate VWAP-anchor+VWAP-loss pairing (entry trigger = M5-close price, VWAP = location context only, OR15-boundary = always-computed contextual secondary never a VWAP-loss substitute), removed unused D1 ATR from mandatory evidence, corrected the freshness clock to `now − evidence_as_of` — zero calibration evidence reopened — status **CONTRACT CORRECTED**. **Owner/Chief Architect approved and closed ID-7B, ID-7B.1, ID-7B.2, and ID-7B.2.1 2026-09-04 (all same day) — V0 methodology fully frozen** — and authorized **ID-7A** (domain model + persistence contract only — explicitly not the evaluator or workflow wiring) same day.

**ID-7A (2026-09-05): IMPLEMENTATION COMPLETE — READY FOR OWNER / CHIEF ARCHITECT REVIEW.** New `entry_actionability_models.py`: immutable `EntryActionability` — persisted state exactly `UNKNOWN`/`NOT_ACTIONABLE`/`ACTIONABLE` (no `EXPIRED`/`STALE`/`CURRENT`/`SUPERSEDED`, preserving ID-7A0.1's own correction), persisted reasons exactly `UPSTREAM_DECISION_NOT_TRADE`/`UPSTREAM_EQ_NOT_QUALIFIED`/`INSUFFICIENT_EVIDENCE`/`INVALIDATION_UNAVAILABLE`, identity = the full upstream `EntryQualification` composite key copied verbatim plus this artifact's own `entry_actionability_as_of`/`entry_actionability_methodology_version` (no surrogate id), `DEFAULT_METHODOLOGY_VERSION="entry-actionability-v0"` minted (deferred by ID-7B.2.1, decided here), `EntryReference`/`EntryLocationContext`/`OperativeInvalidation`/`OpeningRangeContextReference`/`RewardReference` value objects implementing the frozen V0 contract exactly (M5-close entry trigger, VWAP-loss invalidation paired non-degenerately against it, always-optional OR15-boundary context, T1/T2 goal-band reward with informational-only RR, no D1-ATR anywhere, no extension gate), frozen Python constants `T1_GOAL_BAND_PCT`/`T2_GOAL_BAND_PCT`/`CURRENTNESS_MAX_EVIDENCE_AGE_SECONDS=600.0`, a direction-aware structural risk-geometry guard (rejects zero/wrong-side risk distance only — not a calibrated minimum), `Direction` reused bidirectionally, `EntryEvidenceFinality` reused directly from EQ (no duplicate enum). New `entry_actionability_currentness.py` (deliberately separate module — dimension B is never table columns): pure `is_currently_usable(...)` with an injected `now`, exact full-composite-EQ-identity comparison (never `decision_id` alone), the frozen strict `now − evidence_as_of > 600s → STALE` predicate, and a REGULAR-session requirement — never mutates or is confused with the persisted state. `schema.py`: `SCHEMA_VERSION` 17→18, new `entry_actionabilities` table (23 columns, full 7-column composite `PRIMARY KEY`, single-column FK on `decision_id` mirroring `entry_qualifications`' own pattern, value objects as nested-JSON columns mirroring the existing `trade_plan_json` precedent, two supporting indexes). `SqliteRepository` gained `save_entry_actionability` (append-only, idempotent, TWO independent binding validations — canonical Decision agreement mirroring EQ's own check, plus a new proof that the referenced exact upstream `EntryQualification` observation genuinely exists and its denormalized state agrees), `get_entry_actionability`, `latest_entry_actionability_for_entry_qualification` (exact-EQ-identity, never decision-id-alone), `latest_entry_actionability_for_instrument_session`, `list_entry_actionabilities_for_instrument_session`. 75 new tests across 3 new files (`tests/market_intel/test_entry_actionability_models.py`, `tests/market_intel/test_entry_actionability_currentness.py`, `tests/data_layer/test_entry_actionability_repository.py`); full suite 3455 passed / 1 pre-existing unrelated skip / 0 failures. Explicitly absent, source-scan-verified: no `EntryActionabilityEngine` (ID-7C), no workflow/API/UI wiring (ID-7E), no replay/shadow (ID-7F), no production actionability rows, no provider calls, no D1-ATR field, no extension-gate field/reason code — zero touches to `WorkflowStage`, `Decision`/ID-6, ingestion, EMR, or DarvaX. **Owner/Chief Architect source review (2026-09-05): core implementation, domain/schema-v18/repository direction, and ADR-015 compliance ACCEPTED; final closure HELD for narrow domain-integrity corrections — ID-7A.1 authorized and completed same day.** `EntryActionability.__post_init__` now additionally rejects an ACTIONABLE verdict whose `decision_type`/`entry_qualification_state` are not truthfully TRADE/QUALIFIED, an ACTIONABLE verdict carrying any non-empty `reason_codes`, a `NOT_ACTIONABLE`/`UNKNOWN` reason drawn from the wrong semantic family (new `UPSTREAM_ELIGIBILITY_REASON_CODES`/`EVIDENCE_SUFFICIENCY_REASON_CODES` frozensets — no new reason codes, no evaluator-precedence rule invented), an untruthful upstream reason (e.g. `UPSTREAM_DECISION_NOT_TRADE` while `decision_type == TRADE`), and a point-in-time causal-ordering violation (`entry_actionability_as_of < entry_qualification_as_of` or `evidence_as_of > entry_actionability_as_of` — equality and a later re-evaluation both remain valid, per ADR-015's Option-1 extensibility). `is_currently_usable` now rejects `now < evidence_as_of` as an invalid temporal invocation (`ValueError`) rather than silently misclassifying future evidence as CURRENT via a negative age — no new currentness label. `EntryQualificationIdentity` gained structural validation; `RewardReference` rejects negative RR; `save_entry_actionability` rejects a naive `persisted_at` before any binding/insert. `SCHEMA_VERSION` stays 18 (no migration needed) and every repository method signature/query semantic is unchanged. 29 new tests (illegal-ACTIONABLE-upstream rejection, reason-family consistency, temporal-ordering boundaries, currentness future-evidence rejection, identity validation, `persisted_at` regression, and an explicit proof a real WATCH Decision + real matching QUALIFIED EQ still cannot be wrapped in an illegal ACTIONABLE artifact). Full suite: 3484 passed / 1 pre-existing unrelated skip / 0 failures. Status **ID-7A.1 COMPLETE — ID-7A READY FOR OWNER / CHIEF ARCHITECT CLOSURE REVIEW**, not marked Owner-approved. **ID-7C/ID-7D/ID-7E/ID-7F all NOT STARTED, NOT AUTHORIZED** — each needs its own separate owner authorization, mirroring ADR-013's ID-6A0→ID-6E gated sequence; still no API/UI; outcome/profitability validation remains untested |
| Explosive Move research (isolated, ADR-012) | `explosive_move/` — EM-1a immutable event/readiness contracts, EM-1r2 read-only corporate-action coverage/cohort evidence, EM-1r3 provider-free exact-slot intraday reconstruction/manifests, EM-1r4 survivor-cohort admission/quote-timestamp hygiene, EM-1r5's corporate-action boundary-crossing rule, and EM-1b's forward-label contract (`event_labels.py`) and frozen chronological TRAIN/VALIDATION/CALIBRATION/FINAL_TEST partition contract (`partitions.py`); a real deterministic label dataset exists at `artifacts/research/em1b/` (git-ignored), generated by `src/athena/data/em1b_label_dataset_generation.py`. **EM-5 (owner-approved / closed 2026-09-01; contract accepted — check `docs/MILESTONES.md` before relying on this):** `explosive_move/live/` — the v1 replayable live scanner (`run_scan_cycle`) plus the contract §14 fail-fast production canary gate (`canary_gate.py`), reading promoted artifacts from `config/emr/frozen_models/` (`src/athena/data/em5_artifact_promotion.py`) and writing to `explosive_move/store/`'s own isolated `db/emr.db`; Tuesday 2026-09-01 Track B live capture completed for all nine frozen checkpoints; Track B.1 is owner-accepted and classifies the complete zero-off-grid canary as `NO_OFF_GRID_PROVISIONAL_OBSERVED`; the final Section 14 nine-checkpoint canary passed against `athena_core` / 2026-08-28 with zero provider/network calls. **EM-6 (owner-approved / closed 2026-09-03):** `explosive_move/live/presentation.py` (read-only presentation/query functions) plus an isolated `GET /api/v1/emr/experimental/touch-10-radar` API route and a permanently "Experimental" Market Intelligence dashboard panel — no scanner scheduling, no canonical coupling; `db/emr.db` remains absent in production (no scheduler authorized, not a defect). **EM-7A0 (ADR-014) ACCEPTED; EM-7A OWNER APPROVED / CLOSED 2026-09-03 (via EM-7A.1 + EM-7A.2); EM-7B OWNER APPROVED / CLOSED 2026-09-04 (via EM-7B.1); EM-7C CONTROLLED PRODUCTION ACTIVATION AUTHORIZED, CANARY EVIDENCE ACCEPTED, CLOSURE HELD FOR EM-7C.1; EM-7C.1 PRODUCTION MOUNT FAIL-CLOSED ISOLATION COMPLETE — EM-7C READY FOR OWNER / CHIEF ARCHITECT CLOSURE REVIEW; EM-7D NOT AUTHORIZED** (2026-09-03/04): `docs/research/EM-7-DISCOVERY.md` found the gap after EM-6 is entirely operational; owner ratified 5 architecture decisions and accepted `docs/adr/ADR-014-emr-live-shadow-operation.md` — an isolated, config-gated EMR scheduled worker owned entirely by `athena.explosive_move`. EM-7A's mandatory pre-implementation persistence audit found `save_candidates`/`save_transitions`/the terminal `COMPLETE` write were three separate SQLite transactions, contradicting ADR-014 §15's original atomicity assumption (a failure between any two could leave a durable partial result) — reported per explicit owner instruction rather than silently resolved (`docs/research/EM-7A-SCANNER-CORRECTNESS-HARDENING.md`); that day EM-7A independently completed an EMR-owned `flock` concurrency lock (`explosive_move/live/scan_lock.py`), mandatory `regime_lookup` wiring (no more silent all-UNKNOWN fallback), and extended isolation-test coverage, then stopped on lifecycle work. **EM-7A.1 (same day) is the owner's resolution — Option 1 selected, `PARTIAL` lifecycle state rejected:** `EmrRepository.commit_scan_result` now wraps candidate + transition + terminal-`COMPLETE` persistence in one atomic SQLite transaction (`scanner.py`'s `run_scan_cycle` calls it once, replacing the three prior separate calls); `mark_scan_failed` writes explicit bounded `FAILED` diagnostics for every run-level exception, exception-chaining a secondary failure without masking the original; three same-`run_id` idempotency cases (`COMPLETE`→reconstruct with zero recompute, `RUNNING`→reject via `EmrScanAlreadyRunningError`, `FAILED`→retry same `run_id`) are checked before any provider call; a `UNIQUE(run_id, instrument_id, family, threshold_percent)` schema-v2 index is defense-in-depth only; `run_scan_cycle_with_lock` is the one hardened entrypoint a future EM-7B worker must use. Atomicity proven by mutation/negative test (reverted). ADR-014 §15/§16 corrected, preserving the original claim and the EM-7A finding that disproved it. **Owner/Chief Architect source review then accepted EM-7A.1's technical implementation but held EM-7A closure for one narrow mismatch: `run_scan_cycle` still persisted a fourth run status, `SKIPPED_SESSION_TYPE`, on a non-scannable session (writing `RUNNING` then immediately overwriting it), contradicting ADR-014's accepted two-terminal-outcome model. EM-7A.2 (same day) is the owner's resolution:** session-scannability is now checked as a true preflight — after the existing-run dispatch (COMPLETE/RUNNING/FAILED-or-legacy-SKIPPED, unchanged) but before any `RUNNING` write, provider call, or computation; a non-scannable session returns an in-memory-only `SKIPPED_SESSION_TYPE` outcome and persists nothing. New executions can persist only `RUNNING`/`COMPLETE`/`FAILED`; a pre-EM-7A.2 database's legacy `SKIPPED_SESSION_TYPE` rows remain readable (same-`run_id` lookup only, never written again). COMPLETE-authoritative, RUNNING-rejection, and FAILED-row-not-mutated-by-a-skip-retry semantics all preserved, proven by 6 new tests plus a required mutation/negative proof (reverted). **Owner/Chief Architect closed EM-7A.2 and EM-7A themselves 2026-09-03 and authorized EM-7B same day. EM-7B implements the isolated EMR scheduling/invocation layer** (`explosive_move/live/worker.py`, `docs/research/EM-7B-ISOLATED-SCHEDULING-INVOCATION.md`): a pure, fully injectable `run_once` tick plus a thin `EmrWorker` daemon-thread wrapper mirroring `CycleWorker`'s shape without importing it, gated on a new `config/emr/operational.json` (`enabled: false` shipped). Two design decisions audited (not assumed) before deciding: checkpoint-due/catch-up policy (a source search found no requirement to back-fill missed checkpoints, so the owner's own stated preference — latest-due-only — is implemented directly) and universe-policy wiring (a new `_MatureHistoryMarketDataPort` wrapper resolves the frozen mature-history filter without touching `run_scan_cycle`'s own contract, since that contract has no parameter for an explicit instrument-id list). `compute_run_id` extracted from `scanner.py` as a small, behavior-preserving public function. FAILED checkpoints are never auto-retried by the worker's own polling. 3 required mutation/negative proofs (mature-filter bypass, FAILED-retry-gate removal, forbidden import) all caught correctly, reverted. No worker mounted into any production process; `db/emr.db` remains absent. **Owner/Chief Architect source review provisionally accepted EM-7B but held closure for one narrow configuration-authority issue** (2026-09-04): `max_checkpoint_price_delay_seconds` was operator-tunable in `config/emr/operational.json` (`300.0`) even though that value exactly equals `checkpoint_reference_price.MAX_CHECKPOINT_OBSERVATION_DELAY_SECONDS`, an owner-approved bound whose own docstring says "EM-5 must not dynamically retune this." **EM-7B.1 (same day) is the owner's resolution:** the field is removed entirely; `worker.py` now imports and uses the frozen constant directly, unreachable from any config edit. `max_staleness_minutes` stays configurable (genuinely operational per `eligibility.py`'s own docstring), regression-tested against the real accepted canary's own signature default. `base_universe`/`model_version` remain selectors but `load_emr_operational_config` now rejects any value not resolving to an already-frozen source (`athena_core` per ADR-014 §11; a `model_version` whose promoted manifest exists and agrees) — validated only at the config-file boundary, never at direct `EmrOperationalConfig(...)` construction (used extensively by the test suite's own fixtures). Mutation/negative proof performed and reverted. No ADR-014 change needed. **Owner/Chief Architect closed EM-7B.1 and EM-7B themselves 2026-09-04 and authorized EM-7C same day — the FIRST controlled production activation of the isolated EMR live-shadow path** (`docs/research/EM-7C-PRODUCTION-ACTIVATION-CANARY.md`). Added `_mount_emr_worker` (`src/athena/cli.py`), mounted alongside (never through) the canonical `CycleWorker`, gated on `EmrOperationalConfig.enabled` with zero side effects when disabled (mutation-proven). Real production service gracefully restarted via the official `./athena-serve` wrapper with EMR config activated (`athena_core`/`v1`/`30.0`/`30.0`, unchanged from EM-7B.1); `db/emr.db` created intentionally (schema v2). **The genuine scheduled canary occurred naturally at the 09:20 IST checkpoint** (`EmrWorker → run_once → run_scan_cycle_with_lock → run_scan_cycle`) and persisted `COMPLETE`, atomic, with an honest zero-eligible result — 0 of 518 mature-history instruments had canonical M5 candle data yet at that instant, root-caused to canonical ingestion's own scheduling not yet having landed the day's first bar, a genuine cross-system timing characteristic, not an EM-7A/B/C defect. Real Kite `/quote` traffic occurred correctly (zero *accepted* observations exceeding the frozen bound this run, since zero candidates were assembled at all). Canonical DB/runtime, ID-7P0, and DarvaX all verified isolated and unaffected. `enabled=true` left in place for natural EM-7D evidence accumulation (not yet started). **Owner/Chief Architect source review accepted the canary as genuine but held EM-7C closure for one narrow isolation defect: `_mount_emr_worker` loaded/validated `EmrOperationalConfig` outside its own protective `try` block, so an EMR-specific config failure could have crashed canonical `_cmd_serve`. EM-7C.1 (same day) is the owner's resolution** — the whole EMR mount sequence (config load, `enabled` check, canonical repo open, `EmrRepository` init, calendar/tzinfo, `EmrWorker` construct/start) now lives inside one `try` block, fail-closed for EMR, fail-open for canonical ATHENA; invalid config surfaces as one bounded warning, never a silent disabled reinterpretation. 7 new tests plus a required mutation/negative proof (config load moved back outside the boundary; reverted, confirmed identical); incidentally fixed a `test_em7b_worker.py` lock-path collision with the now-real production `EmrScanLock`. No new canary triggered; no production restart performed solely for this fix. Canonical score input/production recommendation remain not implemented |
| Orchestration & Ops | `orchestration/`, `scanner/`, `watchlist/`, `strategy/`, `backtest/`, `scheduling/` |
| Portfolio & planning (no execution) | `portfolio/` — existing portfolio engine plus frozen My Portfolio V1 contracts (`my_portfolio_contracts.py`), PS-P2 generic import parsing/resolution (`imports.py`), PS-P4 sync orchestration (`sync.py`), PS-P5B/PS-P7B/PS-P8C interpretation (`interpretation.py`), PS-P7B confidence adapter (`confidence_adapter.py`), and PS-P8C D1 Trend adapter (`trend_adapter.py`) for CSV/XLSX current-holdings preview, canonical symbol resolution, reconciliation, server-owned snapshot math, background sync state, immutable analysis snapshots, freshness, provenance, holdings-digest currentness (`CURRENT` / `STALE_HOLDINGS_CHANGED` / `UNKNOWN`), Status, Key Trigger, Major Support / Exit, Target 1, Next Action, EntryQualification-gated ADD, inclusive stop-driven EXIT, PARTIAL/FAILED handling, stale snapshot preservation, compact explanation UX, Conviction populated only from coherent persisted Decision Confidence HIGH/MEDIUM/LOW, and Trend populated only from coherent holding D1 SMA20/SMA50 evidence as `UPTREND`/`DOWNTREND`/`MIXED` under `portfolio-interpretation-v2`. My Portfolio V1 is COMPLETE AND FROZEN as of 2026-09-03 after PS-P6C; PS-P7A/P7B/PS-P8A/PS-P8B/PS-P8C/PS-P9A are Owner/Chief Architect approved and frozen; PS-P9B Opening Range Setup methodology/replay is ready for Owner/Chief Architect review and recommends keeping production Setup deferred until directionality, OR15/OR30 precedence, returned-inside behavior, and lifecycle are frozen. Setup, Support 1, Target 2/3, REDUCE, ROTATE, allocation, ranking, and history remain deferred. Isolated `portfolio_*` SQLite tables live in `data/store/schema.py`, API endpoints live under `/api/v1/my-portfolio`, dashboard UX lives in `api/static/js/08b-my-portfolio.js` and `api/static/css/05b-my-portfolio.css`, and `owner_positions` remains the legacy/manual-fill ledger. Also `allocation/`, `sizing/`, `orders/` (planning only), `brokers/` (abstraction only), `execution/` |
| Presentation | `dashboard/`, `explainability/`, `timeline/`, `monitoring/`, `export/`, `analytics/` |
| Application platform | `api/` — FastAPI v1 REST + security + platform; `api/static/` — the dashboard SPA (`index.html`; `dashboard.js` and `dashboard.css` are each served-assembled from concern-based source files — `js/*.js` (concatenated server-side by an `app.py` route) and `css/*.css` (loaded via `@import`) respectively — hand-rolled, no framework/build step per ADR-004) |
| DarvaX satellite (opt-in, ADR-010) | `darvax/` — self-contained: own `config`, `store` (own `db/darvax.db`, own schema version), `ports`/`adapters` (read-only view of ATHENA candles), `primitives`, `signals`, `scan`, `screening` (tiers, universe sweep, action classification, liquidity, calendar-aware daily-sweep freshness), `positions` (owner-recorded holdings; stop-loss frozen at entry, never deleted on close), `validation` (DX-5 outcome harness), `api` (own sub-app, own static assets — three views: Advisor / Levels / Table). ATHENA's only knowledge of it is `api/darvax_mount.py`, which reads activation configuration and injects a read-only host session-calendar port; DarvaX never imports ATHENA calendar/config modules directly |
| Live ops | `notifications/`, `diagnostics/`, `ops/` (owner candidate lists, live validation pipeline, alerts, owner-triggered full-universe validation job) |
| Cross-cutting | `runtime/` (workflow/execution models shared across phases) |

## 7. Stack quick facts

Python (pydantic config, `Decimal` money, tz-aware `datetime`), SQLite
(WAL), FastAPI (localhost-only), static HTML + vanilla JS dashboard (no
frontend framework), in-house indicators (no external TA-lib), pytest.
Broker: Zerodha Kite Connect (DD-1), read-only market data — no order API
integration exists.

## 8. Mandatory workflow (full detail in `CLAUDE.md` — this is the headline)

- **AI never runs git actions** (add/commit/push/etc.) unless the owner
  explicitly asks in that specific instance. Provide a consolidated commit
  message instead — `<type>(<scope>): <summary>` + WHAT/WHY bullets.
- **One milestone in flight at a time.** Design → Implement → Test →
  Self-validate → Milestone Review Summary → owner approval → next. Never
  auto-continue past an approval gate.
- Update `IMPLEMENTATION_SUMMARY.md` after every completed milestone, and
  `docs/MILESTONES.md`'s status column the moment the owner approves.
- **When a track/phase closes, or the module map in §6 changes materially,
  update this file's §6 in the same change set.** This is the one part of
  this briefing that can drift — keep it honest.

## 9. Absolute prohibitions

- No order-placement code, under any milestone, ever.
- No architecture change without an ADR + explicit owner approval.
- No secrets outside `.env`.
- No git actions without explicit, per-instance owner request.
- No skipping the milestone approval gate "to save time."
