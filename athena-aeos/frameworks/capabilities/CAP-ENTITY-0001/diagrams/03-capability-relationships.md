Role
 │
 └──── IMPLEMENTS ─────────► Capability

Capability
 │
 ├──── USED_IN ────────────► Workflow
 ├──── PRODUCES ───────────► Artifact
 ├──── CONSUMES ───────────► Artifact
 ├──── GOVERNED_BY ────────► Policy
 ├──── REFERENCES ─────────► Specification
 └──── DEPENDS_ON ─────────► Capability